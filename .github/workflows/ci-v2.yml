name: V2/V2B/V3/V3B/V4 compile checks

on:
  push:
    branches:
      - v2-state-ledger-reconciliation
      - v2b-kafka-outbox-events
      - v3-security-consent-sca
      - v3b-api-gateway-zero-trust
      - v4-observability-e2e
  pull_request:
    branches:
      - v1-single-immediate
      - v2-state-ledger-reconciliation
      - v2b-kafka-outbox-events
      - v3-security-consent-sca
      - v3b-api-gateway-zero-trust

jobs:
  compile:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module:
          - services/api-gateway
          - services/payment-service
          - services/consumer-psp
          - services/event-audit-service
          - mocks/mock-wero
          - mocks/mock-sct-inst
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Compile ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: mvn -B clean package -DskipTests
