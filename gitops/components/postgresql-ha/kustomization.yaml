apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Reusable CloudNativePG HA database scaffold for V7 C2.
# Environment overlays remain responsible for namespace, zone spread,
# sizing/storage decisions, secrets and operator/CRD prerequisites.
resources:
  - cluster.yaml
